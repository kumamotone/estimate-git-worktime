{
  "name": "estimate-git-worktime",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "description": "A tool that analyzes the commit history of a Git repository to estimate the project's working hours.",
  "dependencies": {
    "express": "^4.21.1"
  }
}
